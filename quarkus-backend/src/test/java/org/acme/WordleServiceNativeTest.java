package org.acme;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class WordleServiceNativeTest extends WordleServiceTest {

    // Execute the same tests but in native mode.
}